# Copy this to .env and fill in real values — never commit .env

# Session signing — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=replace-with-32-char-random-string

# Google Workspace SMTP Relay
SMTP_HOST=smtp-relay.gmail.com
SMTP_PORT=587
SMTP_USER=jp@topsisconsulting.com
SMTP_PASSWORD=replace-with-google-app-password
EMAIL_FROM=info@topsisconsulting.com
EMAIL_FROM_NAME=Topsis Consulting

# Jira Cloud
JIRA_URL=https://topsis.atlassian.net
JIRA_USER_EMAIL=jp@topsisconsulting.com
JIRA_API_TOKEN=replace-with-jira-api-token

# Redis — for local dev, run: docker run -p 6379:6379 redis
REDIS_URL=redis://localhost:6379

# Salesforce — read-only Opportunity milestone dates.
# Production: Connected App + JWT Bearer flow. Private key comes from Secret Manager.
SF_LOGIN_URL=https://login.salesforce.com
SF_API_VERSION=v60.0
SF_CLIENT_ID=replace-with-connected-app-consumer-key
SF_USERNAME=integration-user@topsisconsulting.com
# PEM contents; injected from Secret Manager in prod.
SF_PRIVATE_KEY=
# Local dev only: paste a token from `sf org display --target-org prod_org --json`.
# Leave blank in production — the JWT flow above is used instead.
SF_ACCESS_TOKEN=
SF_INSTANCE_URL=https://topsisconsulting.my.salesforce.com

# Set to "true" in local dev to skip actual email sending (prints OTP to console instead)
DEV_MODE=true
